const styles = {
  colors: {
    black: "#4a4a4a",
    gray: "#808080",
    blue: "#209ceed1"
  },
  border: {
    radius: "5px",
    withColor: color => `1px solid ${color}`
  }
};

export default styles;
